<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE part PUBLIC "-//OASIS//DTD DocBook XML V4.4//EN"
  "../docbook-xml/docbookx.dtd"
[
<!ENTITY % myents SYSTEM "../fvwm.ent" >
%myents;
]>

<!-- $Id$ -->

<section id='Deschedule'>
<title>Deschedule</title>

<cmdsynopsis>
	<command>Deschedule</command
	><arg choice='opt'
		><replaceable>command_id</replaceable
	></arg>
</cmdsynopsis>

<para>Removes all commands that were scheduled with the id
<replaceable>command_id</replaceable>
with the
<fvwmref cmd="Schedule"/>
command from the list of commands to be executed unless they were
already executed.  If the
<replaceable>command_id</replaceable>
is omitted, the value of the variable $[schedule.last] is used as
the id.</para>

</section>
