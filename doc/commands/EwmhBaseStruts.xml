<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE part PUBLIC "-//OASIS//DTD DocBook XML V4.4//EN"
  "../docbook-xml/docbookx.dtd"
[
<!ENTITY % myents SYSTEM "../fvwm.ent" >
%myents;
]>

<!-- $Id$ -->

<section id='EwmhBaseStruts'>
<title>EwmhBaseStruts</title>

<cmdsynopsis>
	<command>EwmhBaseStruts</command>
	<arg choice='plain'><replaceable>left</replaceable></arg>
	<arg choice='plain'><replaceable>right</replaceable></arg>
	<arg choice='plain'><replaceable>top</replaceable></arg>
	<arg choice='plain'><replaceable>bottom</replaceable></arg>
</cmdsynopsis>

<para>Where left, right, top and bottom are positive or null integers
which define bands at the edge of the screen.
<replaceable>left</replaceable>
defines a band on the left of your screen of width
<replaceable>left</replaceable>,
<replaceable>right</replaceable>
defines a band on the right of your screen of width
<replaceable>right</replaceable>,
<replaceable>top</replaceable>
defines a band on the top of your screen of height
<replaceable>top</replaceable>
and
<replaceable>bottom</replaceable>
defines a band on the bottom of your screen of height
<replaceable>bottom</replaceable>.
The unit is the pixel and the default is 0 0 0 0.  These areas
define additional reserved space to the reserved space defined by
some ewmh compliant applications. This is used to compute the
Working Area. See the
<fvwmref sect="ewmh" opt="extended_window_manager_hints" name="Extended Window Manager Hints"/>
section for a definition of the Working Area.</para>

</section>
