name: FVWM3 CI ISSUES

on:
    issues:
        types: [ opened, edited, milestoned, closed ]

jobs:
    notification:
        runs-on: ubuntu-latest
        name: notifications
        steps:
            - name: issue notifications
              uses: rectalogic/notify-irc@v1
              with:
                  channel: "#fvwm"
                  nickname: fvwm3-gh
                  message: "Issue [${{ github.issues.state }}]: [${{ github.issues.number }}]: ${{ github.issues.title }} -- [${{ github.issues.user.login }}]"
