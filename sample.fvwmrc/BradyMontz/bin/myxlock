#!/bin/sh

# Useful things to set XLOCKCOMMAND to:
# /usr/X11R6/bin/xset s activate
#  or
# xscreensaver-command -activate
#  or
# xscreensaver-command -lock

if [ -z "$XLOCKCOMMAND" ]
then
	xlock -usefirst
else
	eval $XLOCKCOMMAND
fi
